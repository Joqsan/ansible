- name: Install core packages
  apt:
    name: "{{ item }}"
    state: latest
  with_items:
      - ca-certificates
      - gcc
      - ninja-build
      - gettext
      - cmake
      - unzip
      - curl
      - git
      - locales
      - nodejs
      - npm
      - ripgrep
  tags:
    - core